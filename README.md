💬 AI Customer Support Agent

A Python-based customer support chatbot built with **Streamlit** and **Pandas**. This bot loads customer support conversations from a CSV dataset (Kaggle Customer Support Dataset) and retrieves relevant answers based on user queries.

🚀 Features

* **Instant Responses:** Matches user questions to a pre-loaded knowledge base.
* **Data Preview:** Includes a built-in "Cheat Sheet" sidebar/preview to view valid questions from the dataset.
* **Keyword Matching:** Uses text analysis to find the best matching response for customer queries.
* **Fallback Handling:** Provides helpful error messages if a question isn't found in the database.

🛠️ Tech Stack

* Python 3.x
* Streamlit (for the web interface)
* Pandas (for data handling and CSV parsing)
